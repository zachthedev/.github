{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "A Rust workspace that publishes nothing to crates.io and ships a built artifact, such as a game mod. Same rules as rust-crates. The difference is what the repository adds, such as a submodule pointer or a crate it consumes from crates.io."
  ],
  "extends": ["./rust-crates"]
}

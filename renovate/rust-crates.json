{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "A Rust workspace that publishes crates to crates.io and ships a built artifact. Versions live in [workspace.dependencies], which mixes runtime and development crates. A workspace bump is therefore a fix, and the repository names its development-only crates as chore.",
    "The toolchain pin is exact, moves on its own branch and is a fix, by the rule below. The cargo manager's own commit topic is overridden, so a header reads bump serde, not bump Rust crate serde.",
    "osvVulnerabilityAlerts reads OSV, which carries RustSec entries GitHub's database lacks. Renovate marks the option experimental."
  ],
  "extends": ["./default"],
  "osvVulnerabilityAlerts": true,
  "cargo": {
    "rangeStrategy": "bump",
    "commitMessageTopic": "{{depName}}"
  },
  "packageRules": [
    {
      "matchManagers": ["cargo"],
      "matchDepTypes": ["dependencies", "workspace.dependencies"],
      "semanticCommitType": "fix"
    },
    {
      "description": "The toolchain builds the artifact this workspace ships, and the standard library links into that binary, so a bump ships.",
      "matchManagers": ["rust-toolchain"],
      "matchDepTypes": ["toolchain"],
      "rangeStrategy": "pin",
      "semanticCommitType": "fix"
    }
  ],
  "lockFileMaintenance": {
    "semanticCommitType": "fix"
  }
}
